class AIProviderService:

    @staticmethod
    def generate(
        prompt: str,
    ) -> str:

        return (
            "AI provider integration "
            "will be added in the next sprint."
        )